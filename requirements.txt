# Core Web Framework
fastapi==0.110.0
uvicorn[standard]==0.29.0

# PDF Parsing
PyMuPDF==1.23.22  # Also known as fitz

# Embeddings
sentence-transformers==2.6.1
numpy==1.26.4

# Vector Database
qdrant-client==1.7.2

# HTTP requests
httpx==0.27.0

# Environment variable support (optional)
python-dotenv==1.0.1

# Optional if you plan to run local LLMs (you'll call Ollama via HTTP)
# Ollama not installed via pip – you need to have it installed locally from https://ollama.com
